<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
                 http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                 http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- Let users authenticate using Spring Security's form login -->
    <http auto-config="true" use-expressions="true">

        <!-- Session-based login -->
        <session-management>
            <concurrency-control max-sessions="1"/>
            <session-fixation-protection/>
        </session-management>

        <!-- Public endpoints (allow unauthenticated access) -->
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/index.jsp" access="permitAll"/>
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/public/**" access="permitAll"/>

        <!-- Admin API -->
        <intercept-url pattern="/api/admin/**" access="hasRole('ADMIN')"/>

        <!-- Everything else requires login for now (can be relaxed once controllers exist) -->
        <intercept-url pattern="/**" access="permitAll"/>

        <form-login
                login-page="/login"
                login-processing-url="/login"
                default-target-url="/api/admin/dashboard"
                authentication-failure-url="/login?error=true"
                username-parameter="username"
                password-parameter="password"/>

        <logout logout-url="/logout" logout-success-url="/login?logout=true"/>

        <!-- CSRF is enabled by default; keep it for now. If React fails with 403, we can disable for REST endpoints. -->
        <csrf/>

    </http>

    <!-- Custom user details service and auth provider -->
    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="passwordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>

